# This monorepo uses separate .env files for each app:
#
# - apps/api/.env       - Backend API configuration
# - apps/web/.env       - Frontend configuration
#
# Copy the .env.example files in each directory to get started:
#
#   cp apps/api/.env.example apps/api/.env
#   cp apps/web/.env.example apps/web/.env
#
# Then edit each file with your configuration values.
